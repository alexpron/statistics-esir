```R
# Read data from file
data <- read("td1ex1_data.data",header=F, sep=',', dec='.')

# Compute mean and quartiles
mean(data)
quantile(data, probs=seq(0,1,0.25))
# get directly summary statistics
summary(data)

# Boxplot
boxplot(data)
# Outliers appear outside the sticks of the boxplot
```

